const eventEmitter = require("../../../emitter");

module.exports = (socket, io) => {
  /**
   * roleCreated
   * reviewerAssigned
   * reviewerRemoved
   *
   */
  // eventEmitter.on("roleCreated", (data) => {
  //   console.log({ roleCreated: true });
  //   socket.emit("roleCreated", data);
  // });
  // eventEmitter.on("reviewerAssigned", (data) => {
  //   console.log({ reviewerAssigned: true });
  //   socket.emit("reviewerAssigned", data);
  // });
  // eventEmitter.on("reviewerRemoved", (data) => {
  //   console.log({ roleCreated: true });
  //   socket.emit("reviewerRemoved", data);
  // });
};
